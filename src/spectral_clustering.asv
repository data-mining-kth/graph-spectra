% Implementation of the algorithm
% Given the set of points S
S = readmatrix('../data/example1.dat');
n = size(S, 1);
x_coord = S(:,1);
y_coord = S(:,2);
% Form the affinity matrix A
A = zeros(n,n);
for i = 1:n
    for j = 1:n
        j = i+1;
    A_ij = exp(-norm(S[i]-S[j], 2)/(2*sigma^2))
end
